<%= form_for @user do |f| %>
    <p>
      <%= f.label :name, 'Name' %>
      <%= f.text_field :name %>
    </p>

    <p>
      <%= f.label :user_name, 'UserName' %>
      <%= f.text_field :user_name %>
    </p>

    <%= f.submit "Update" %>
<% end %>